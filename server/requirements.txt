# API-surface pins. FastAPI / Starlette deprecate and remove things on a
# fast cadence (e.g. add_event_handler, on_event), so unpinned installs
# silently drift between local and production. Each upper bound covers
# what's tested today; lift it deliberately when we adopt a new minor.
fastapi>=0.115,<0.130
starlette>=0.46,<0.50
uvicorn>=0.30,<0.40
pydantic>=2.10,<3.0
fastapi-utils>=0.8,<0.9
sqlalchemy>=2.0,<2.1
httpx>=0.27,<0.29
asyncpg
aiosqlite
asyncmy
jinja2
pytest-asyncio
greenlet
typing_inspect
eval_type_backport
passlib[bcrypt]==1.7.4
# bcrypt 4.1.0+ removes the __about__ module attribute that passlib 1.7.4
# expects when importing the backend. Pin below 4.1 until passlib updates.
bcrypt>=4.0.1,<4.1
ldap3
dnspython
dnslib
pymongo
puresnmp
jmespath
PyJWT
cryptography
h2
pyotp
